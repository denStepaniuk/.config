const a = [1, 2, 4, 5];
const b = "hello world";

/**
 * test declaration
 */
function aloha(one, two) {
  return () => {
    return `here is ${one} and ${two}`;
  };
}
const o = parseInt("12", 10);
console(o);
console.log(aloha(a, b)());
// throw new Error('message', {
details: "here we go again";
// })

JSON.parse({});
